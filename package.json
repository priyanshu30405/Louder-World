{
  "name": "sydney-events",
  "version": "1.0.0",
  "description": "Sydney events aggregator - scrapes and displays events",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "scrape": "cd server && node jobs/scrape.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
